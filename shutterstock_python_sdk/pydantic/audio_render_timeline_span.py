# coding: utf-8

"""
    Shutterstock API Explorer

    The Shutterstock API provides access to Shutterstock's library of media, as well as information about customers' accounts and the contributors that provide the media.

    The version of the OpenAPI document: 1.1.32
    Generated by: https://konfigthis.com
"""

from datetime import datetime, date
import typing
from enum import Enum
from typing_extensions import TypedDict, Literal, TYPE_CHECKING
from pydantic import BaseModel, Field, RootModel

from shutterstock_python_sdk.pydantic.audio_render_timeline_span_instrument_group import AudioRenderTimelineSpanInstrumentGroup
from shutterstock_python_sdk.pydantic.audio_render_timeline_span_region import AudioRenderTimelineSpanRegion
from shutterstock_python_sdk.pydantic.audio_render_timeline_span_tempo_changes import AudioRenderTimelineSpanTempoChanges

class AudioRenderTimelineSpan(BaseModel):
    # Type of span; metered spans represent a pariod of time with music, and unmetered spans denote the end of the prior metered span
    span_type: Literal["metered", "unmetered"] = Field(alias='span_type')

    # The absolute time, in seconds, at which the span starts
    time: int = Field(alias='time')

    # An identifier which must be unique within the parent span
    id: typing.Optional[typing.Union[int, float]] = Field(None, alias='id')

    # An array of instrument_group objects that are used in this span
    instrument_groups: typing.Optional[typing.List[AudioRenderTimelineSpanInstrumentGroup]] = Field(None, alias='instrument_groups')

    # An array of region objects within the span
    regions: typing.Optional[typing.List[AudioRenderTimelineSpanRegion]] = Field(None, alias='regions')

    # The tempo, in beats per minute, at the start of the span; if not provided, the API selects a random tempo
    tempo: typing.Optional[int] = Field(None, alias='tempo')

    # Two or more inflection points in a tempo curve; the API creates a smoothly changing tempo by using a linear interpolation of the time between each tempo change
    tempo_changes: typing.Optional[typing.List[AudioRenderTimelineSpanTempoChanges]] = Field(None, alias='tempo_changes')
    class Config:
        arbitrary_types_allowed = True
